import React from 'react';

const Board = ({boardId}) => {
    return (
        <div>
            <h1>Hello! You are on board with board id {boardId}</h1>
        </div>
    );
};

export default Board;